# Normalize line endings. Committed as LF, checked out native.
# Matters here because commits come from Windows and Vercel builds on Linux.
* text=auto eol=lf

# Binary — never touch.
*.png binary
*.pdf binary
*.woff2 binary
*.ico binary
